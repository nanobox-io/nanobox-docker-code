#!/bin/sh -e
exec 2>&1

export PATH="/data/code/sbin:/data/code/bin:/data/sbin:/data/bin:/opt/gonano/sbin:/opt/gonano/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
<% envs.each do |k, v| -%>
export <%= k %>="<%= v %>"
<% end -%>

cd /data/code
exec <%= exec %>
